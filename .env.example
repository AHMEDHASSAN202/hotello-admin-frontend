# Base URL of the gxp-backend API
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Optional: enables the Google Maps address search in the hotel forms.
# Needs the "Maps JavaScript API" + "Places API (New)" enabled on the key.
# Leave empty to fall back to manual address entry.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
